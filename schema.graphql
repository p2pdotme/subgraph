# CIRCLE
type Circle @entity {
  id: Bytes!

  circleId: BigInt!
  admin: String!
  currency: Bytes!
  name: String!
  communityLink: String!
  isAutoApprovedPCEnabled: Boolean! 

  metrics: CircleMetrics @relation(name: "circle")

  blockNumber: BigInt! 
  blockTimestamp: BigInt!
  transactionHash: Bytes!
}

# STAKER
# An individual who stakes in any circle
type Staker @entity {
  id: Bytes!

  staker: String!

  totalStaked: BigInt!

  circleStakeRecords: [CircleStakeRecords!]!
  circleUnstakeRecords: [CircleUnstakeRecords!]!

  blockNumber: BigInt!
  blockTimestamp: BigInt!
  transactionHash: Bytes!
}

# CIRCLE STAKE RECORDS 
# A record of a staker's stake in a circle
type CircleStakeRecords @entity {
  id: Bytes!

  circle: Circle!
  staker: Staker!
  
  amount: BigInt!

  unstakeRequest: CircleUnstakeRecords

  stakedAt: BigInt!

  blockNumber: BigInt!
  blockTimestamp: BigInt!
  transactionHash: Bytes!
}

# CIRCLE UNSTAKE RECORDS
# A record of a staker's unstake in a circle
type CircleUnstakeRecords @entity {
  id: Bytes!
  
  circle: Circle!
  staker: Staker!
  
  amount: BigInt!
  availableAt: BigInt!

  status: BigInt!
  
  blockNumber: BigInt!
  blockTimestamp: BigInt!
  transactionHash: Bytes!
}

# CIRCLE METRICS
# Metrics for a circle
type CircleMetrics @entity {
  id: Bytes!

  circle: Circle @relation(name: "metrics")

  # STAKING METRICS
  totalStaked: BigInt!
  adminStaked: BigInt!

  # DELEGATION METRICS
  totalDelegatedStake: BigInt!

  # VOLUME METRICS
  totalVolume: BigInt!

  # ORDER METRICS
  totalPlacedOrdersCount: BigInt!
  totalCompletedOrdersCount: BigInt!
  totalCancelledOrdersCount: BigInt!

  # DISPUTE METRICS
  raisedDisputesCount: BigInt!
  resolvedDisputesCount: BigInt!

  # MERCHANT METRICS
  totalMerchantsCount: BigInt!

  blockNumber: BigInt!
  blockTimestamp: BigInt!
  transactionHash: Bytes!
}

# CIRCLE MERCHANT
# A merchant who is registered in a circle
type CircleMerchant @entity {
  id: Bytes!
  merchant: String!
  telegramId: String!
  circleId: BigInt!

  circle: Circle!
  orders: [Orders!]

  stakedAmount: BigInt!
  delegatedStakedAmount: BigInt!

  isOnline: Boolean!
  isBlacklisted: Boolean!
  isOngoingOrder: Boolean!

  onlineAt: BigInt!
  offlineAt: BigInt!
  startedAt: BigInt!

  currency: Bytes!

  # Order metrics for completion rate calculation
  totalOrdersCount: BigInt!
  completedOrdersCount: BigInt!
  cancelledOrdersCount: BigInt!

  paymentChannels: [MerchantPaymentChannels!]
  volumeByMonth: [MerchantVolumeByMonth!]
  paymentChannelMigrations: [PaymentChannelMigration!]!

  blockNumber: BigInt!
  blockTimestamp: BigInt!
  transactionHash: Bytes!
}

# MERCHANT PAYMENT CHANNELS
# A payment channel of a merchant
type MerchantPaymentChannels @entity {
  id: Bytes!
  merchant: CircleMerchant!

  pcConfigId: BigInt!
  accountNo: BigInt!
  label: String!
  isActive: Boolean!
  status: Int!
  isMonthlyVolumeUnlimited: Boolean!
  fiatBalance: BigInt!

  blockNumber: BigInt!
  blockTimestamp: BigInt!
  transactionHash: Bytes!
}

# ORDERS
# A order in a circle
type Orders @entity(immutable: false) {
  id: Bytes!
  orderId: BigInt!
  type: Int!
  status: Int!
  circleId: BigInt!

  userAddress: Bytes!
  usdcRecipientAddress: Bytes!

  usdcAmount: BigInt!
  fiatAmount: BigInt!

  currency: Bytes!

  isUserCompleted: Boolean!
  userCompletedAt: BigInt!
  completedAt: BigInt!
  placedAt: BigInt!
  acceptedAt: BigInt!
  paidAt: BigInt!
  cancelledAt: BigInt!

  acceptedPCId: BigInt!
  acceptedMerchantAddress: Bytes!
  merchant: CircleMerchant
  assignedMerchants: [AssignedMerchants!]

  pubkey: String!
  encUpi: String!
  userPubKey: String!
  encMerchantUpi: String!
  cancelledBy: Bytes!

  # ADDITIONAL ORDER DETAILS
  fixedFeePaid: BigInt!
  tipsPaid: BigInt!
  actualUsdcAmount: BigInt!
  actualFiatAmount: BigInt!

  # DISPUTE DETAILS
  disputeStatus: Int!
  disputeRedactTransId: BigInt!
  disputeAccountNumber: BigInt!
  disputeSettledByAddr: Bytes!
  disputeFaultType: Int!
  disputePlacedAt: BigInt!
  disputeSettledAt: BigInt!

  blockNumber: BigInt!
  blockTimestamp: BigInt!
  transactionHash: Bytes!
}

# ASSIGNED MERCHANTS
# A merchant who is assigned to an order
type AssignedMerchants @entity {
  id: Bytes!

  assignedMerchant: String!
  assignedPCId: BigInt!
  orderId: BigInt!
  merchant: CircleMerchant

  blockNumber: BigInt!
  blockTimestamp: BigInt!
  transactionHash: Bytes!
}

# CIRCLE ADMIN REWARDS
# A rewards for a circle admin
type CircleAdminRewards @entity {
  id: Bytes!
  admin: String!

  lockedRewards: BigInt!
  earnedRewards: BigInt!
  withdrawnRewards: BigInt!
  claimableRewards: BigInt!

  blockNumber: BigInt!
  blockTimestamp: BigInt!
  transactionHash: Bytes!
}

# MERCHANT REWARDS
# A rewards for a merchant
type MerchantRewards @entity {
  id: Bytes!
  merchant: String!

  lockedRewards: BigInt!
  earnedRewards: BigInt!
  withdrawnRewards: BigInt!
  claimableRewards: BigInt!

  blockNumber: BigInt!
  blockTimestamp: BigInt!
  transactionHash: Bytes!
}

# MERCHANT VOLUME BY MONTH
# A volume for a merchant by month
type MerchantVolumeByMonth @entity {
  id: Bytes!

  merchant: CircleMerchant!
  circle: Circle!
  paymentChannel: MerchantPaymentChannels!

  month: String!
  volume: BigInt!

  blockNumber: BigInt!
  blockTimestamp: BigInt!
  transactionHash: Bytes!
}

# PAYMENT CHANNEL MIGRATION
# A record of payment channel migration requests
type PaymentChannelMigration @entity {
  id: Bytes!

  merchant: CircleMerchant!

  fromAccountNo: BigInt!
  toAccountNo: BigInt!
  fromPaymentChannelIndex: BigInt!
  toPaymentChannelIndex: BigInt!

  # Migration status: 0=DEFAULT, 1=PENDING, 2=APPROVED, 3=REJECTED
  status: BigInt!

  # Fiat balance transferred during migration
  fromFiatBalance: BigInt!
  toFiatBalance: BigInt!

  requestedAt: BigInt!
  settledAt: BigInt!

  blockNumber: BigInt!
  blockTimestamp: BigInt!
  transactionHash: Bytes!
}
